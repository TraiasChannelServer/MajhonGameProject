#include "shig_utlity.h"

namespace shig {

	template <class T>
	bool rng_chk(T v, T l, T r) // kore_iru?
		// if (v < l || v > r)return false;
	{
		if (v < l || v > r)return false;
		else return true;
	}








}